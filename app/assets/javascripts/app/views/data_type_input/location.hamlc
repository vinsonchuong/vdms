%label{for: "#{@id}_name"}= @question
%p Please search by location name or address and then select one of the suggestions.
%input.location{id: "#{@id}_name", name: "#{@name}.name", type: 'text', size: 60}
%input.location_id{id: "#{@id}_id", name: "#{@name}.id", type: 'hidden'}
