%form{id: 'edit_host_form'}
  %fieldset
    %h3 Contact Info
    %ul
      %li.field.required
        %label{for: 'person.name'} Name
        %input{id: 'person.name', name: 'person.name', type: 'text', size: 30}
      %li.field.required
        %label{for: 'person.email'} Email
        %input{id: 'person.email', name: 'person.email', type: 'text', size: 30}
  %fieldset
    %h3 Preferences
    %ul
      - for field, fieldNo in @host.fields
        - field_name = "fields[#{fieldNo}].data"
        - field_type = App.HostFieldType.find(field.field_type_id)
        %li.field
          - if field_type.data_type == 'text'
            %label{for: field_name}= field_type.name
            %input{id: field_name, name: field_name, type: 'text', size: 30}
          - else if field_type.data_type == 'single_select'
            %label{for: field_name}= field_type.name
            %select{id: field_name, name: field_name}
              - for option in field_type.options.selection_items
                %option= option
          - else if field_type.data_type == 'multiple_select'
            - field_name += '[]'
            %label= field_type.name
            %ul
              - for option, optionNo in field_type.options.selection_items
                - id = 'option_' + optionNo
                %li
                  %input{type: 'checkbox', name: field_name, id: id, value: option}
                  %label{for: id}= option
  %fieldset
    %button Update
