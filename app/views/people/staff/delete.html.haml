- content_for :title, t('.title', :name => @staff_instance.full_name)
- form_for @staff_instance, :url => {:action => 'destroy'}, :html => {:method => :delete} do |staff_form|
  != staff_form.submit t('.submit')
