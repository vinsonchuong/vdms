- content_for :title, t('.title', :name => @person.name)
= form_for @person, :url => {:action => 'destroy'}, :html => {:method => :delete} do |person_form|
  %fieldset
    != person_form.submit t('.submit')
