- content_for :title, t('.title')
%table
  %thead
    %tr
      %th{:scope => 'col'} Time
      %th{:scope => 'col'} Faculty Name
      %th{:scope => 'col'} Room
  %tbody
    - @admit_meetings.each do |meeting|
      %tr
        %td!= meeting.time
        %td!= meeting.faculty.full_name
        %td!= meeting.room







