- content_for :title, 'Join Event'
%p You are currently not registered for this event.
.section
  = form_tag create_from_current_user_event_hosts_url(@event), :class => 'inline' do
    != submit_tag 'Join as Host'
  = form_tag create_from_current_user_event_visitors_url(@event), :class => 'inline' do
    != submit_tag 'Join as Visitor'
