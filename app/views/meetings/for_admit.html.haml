- content_for :title, t('.title', :name => @admit.full_name)
%table.schedule
  %thead
    %tr
      %th{:scope => 'col'} Time
      %th{:scope => 'col'} Faculty Name
      %th{:scope => 'col'} Room
  %tbody
    - @admit.time_slots.each do |time_slot|
      - meeting = @admit.meeting_at_time(time_slot.begin)
      %tr{:class => (time_slot.available ? nil : 'unavailable')}
        %td!= time_slot.begin.strftime('%I:%M%p')
        - unless meeting.nil?
          %td!= link_to meeting.faculty.full_name, (@current_user.class == Staff ? tweak_faculty_meetings_path(meeting.faculty) : faculty_meetings_path(meeting.faculty))
          %td!= meeting.room
        - else
          %td
          %td
