- content_for :title, 'View Meeting Schedule'
%table
  %thead
    %tr
      %th{:scope => 'col'} Time
      %th{:scope => 'col'} Faculty Name
      %th{:scope => 'col'} Room
  %tbody
    - @admit_meetings.each do |meeting|
      %tr
        %td!= meeting.time
        %td!= meeting.faculty.full_name
        %td!= meeting.room







